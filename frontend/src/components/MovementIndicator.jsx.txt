export default function MovementIndicator() {
  const periods = ['1 h', '8 h', '24 h'];
  return (
    <div className="bg-white rounded p-4 shadow">
      <h3 className="font-semibold mb-2">Expected Movement</h3>
      <div className="flex gap-2">
        {periods.map((p) => (
          <div key={p} className="flex-1 text-center py-2 border rounded">
            {p} <span className="text-green-600">↑ 0.4 %</span>
          </div>
        ))}
      </div>
    </div>
  );
}